<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".cardresult1"></activity>
        <activity android:name=".story" />
        <activity android:name=".register" />
        <activity android:name=".near_res" />
        <activity android:name=".activity_register"></activity>
        <activity android:name=".story" />
        <activity android:name=".near_res" />
        <activity android:name=".awholenewworld" />
        <activity android:name=".memberinfo" />
        <activity android:name=".store2" />
        <activity
            android:name=".activity_introduce_store"
            android:label="@string/title_activity_test"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity_card" /> <!-- <activity -->
        <!-- android:name=".shopinfo" -->
        <!-- android:label="@string/title_activity_shopinfo" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <activity android:name=".search" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".MainActivity">

    <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>